# Disable animations
1. Install: `sudo apt install dconf-editor`
1. Run: `dconf-editor`
1. Search: `animation` and disable

# Edit start menu
1. Use program named Alacarte
	1. Install: `sudo apt install alacarte`
